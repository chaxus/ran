dist/
LICENSE.md
pnpm-lock.yaml
pnpm-workspace.yaml
report
public
assets
.agents/
skills/

# Playwright artefacts — generated, gitignored, and full of JSON that Prettier would
# happily reformat. Without these, `lint:prettier` is clean only until someone runs the
# e2e suite, after which it reports ~180 style issues in files nobody wrote.
test-results/
playwright-report/
coverage/

# Generated documentation. Every file below is produced by a generator that owns its
# exact bytes and is freshness-gated by `pnpm run verify:docs`; Prettier reformatting it
# after generation would make the two checks contradict each other, so only regenerating
# is the source of truth. For `packages/ranuts/docs/API.md` Prettier also actively
# corrupts the content: the illustrative snippet `import { /* … */ } from 'ranuts/utils'`
# gets reformatted to `import {} from /* … */ 'ranuts/utils'`, moving the placeholder
# comment outside the braces.
packages/ranuts/docs/API.md
packages/docs/src/ranuts/api.md
packages/docs/cn/src/ranuts/api.md
packages/ranui/docs/COMPONENTS.md
packages/docs/src/ranui/api.md
packages/docs/cn/src/ranui/api.md
packages/ranui/docs/style-tokens-parts.md
packages/ranui/docs/style-tokens-public.md
