dist/
LICENSE.md
pnpm-lock.yaml
pnpm-workspace.yaml
report
public
assets
.agents/
skills/

# Playwright artefacts — generated, gitignored, and full of JSON that Prettier would
# happily reformat. Without these, `lint:prettier` is clean only until someone runs the
# e2e suite, after which it reports ~180 style issues in files nobody wrote.
test-results/
playwright-report/
coverage/

# Generated API reference (`pnpm -F ranuts doc:api`), emitted to two places. Prettier
# actively corrupts it: the illustrative snippet `import { /* … */ } from 'ranuts/utils'`
# gets reformatted to `import {} from /* … */ 'ranuts/utils'`, moving the placeholder
# comment outside the braces. Regenerating is the only source of truth here.
packages/ranuts/docs/API.md
packages/docs/src/ranuts/api.md
