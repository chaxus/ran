/**
 * The client bundle.
 *
 * Everything on this site renders without it — the stylesheet is bundled here only so
 * that it gets a content hash and a minifier, not because the page depends on script.
 */
import '../styles/site.css';
